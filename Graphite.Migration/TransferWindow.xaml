<Window
    x:Class="Graphite.Migration.TransferWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Transfer to New Graphite PC">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="UserComboBox" Grid.Row="0" Margin="0,0,0,10" DisplayMemberPath="Username"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBox x:Name="DestinationIpTextBox" Grid.Row="2" Margin="0,0,0,10" PlaceholderText="Destination IP Address"/>
        <Button x:Name="TransferButton" Grid.Row="3" Content="Transfer Data" Click="TransferButton_Click" Margin="0,0,0,10"/>
        <ProgressRing x:Name="TransferProgressRing" Grid.Row="4" IsActive="False"/>
        <TextBlock x:Name="StatusTextBlock" Grid.Row="5" TextWrapping="Wrap"/>
    </Grid>
</Window>

