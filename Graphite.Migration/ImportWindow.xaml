<Window
    x:Class="Graphite.Migration.ImportWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Import from Other Browser">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="UserComboBox" Grid.Row="0" Margin="0,0,0,10" DisplayMemberPath="Username"/>
        <ComboBox x:Name="BrowserComboBox" Grid.Row="1" Margin="0,0,0,10">
            <ComboBoxItem Content="Microsoft Edge"/>
            <ComboBoxItem Content="Google Chrome"/>
            <ComboBoxItem Content="Mozilla Firefox"/>
            <ComboBoxItem Content="Brave"/>
        </ComboBox>
        <Button x:Name="ImportButton" Grid.Row="2" Content="Import Data" Click="ImportButton_Click" Margin="0,0,0,10"/>
        <ProgressRing x:Name="ImportProgressRing" Grid.Row="3" IsActive="False"/>
        <TextBlock x:Name="StatusTextBlock" Grid.Row="4" TextWrapping="Wrap"/>
    </Grid>
</Window>

