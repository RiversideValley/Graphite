<Window
    x:Class="Riverside.Graphite.UserDashBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:graphite="using:Graphite.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Graphite"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Graphite - Login"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Resources>
            <Style x:Key="TextButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Padding="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    AutomationProperties.AccessibilityView="Raw"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTransitions="{TemplateBinding ContentTransitions}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="CardButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefault}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid
                                x:Name="RootGrid"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Padding="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    AutomationProperties.AccessibilityView="Raw"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTransitions="{TemplateBinding ContentTransitions}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource CardBackgroundFillColorSecondary}" />
                                                <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource CardBackgroundFillColorTertiary}" />
                                                <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource CardBackgroundFillColorDisabled}" />
                                                <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultDisabled}" />
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorDisabled}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button
            x:Name="SettingsButton"
            Margin="8,8,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Style="{StaticResource TextButtonStyle}">
            <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE713;" />
            <Button.Flyout>
                <Flyout>
                    <StackPanel Width="125" Background="Transparent">
                        <TextBlock
                            Margin="0,0,0,12"
                            FontWeight="SemiBold"
                            Text="Settings" />
                        <ToggleSwitch x:Name="ButtonsToggle" Header="Show Buttons" />
                        <ToggleSwitch x:Name="CheckboxToggle" Header="Show Checkbox" />
                        <ToggleSwitch x:Name="WeatherToggle" Header="Show Weather" />
                    </StackPanel>
                </Flyout>
            </Button.Flyout>
        </Button>

        <TextBlock
            Margin="0,48,0,0"
            HorizontalAlignment="Center"
            FontSize="32"
            FontWeight="SemiBold"
            Text="Who's using Graphite?" />

        <Grid
            Margin="0,48,12,0"
            HorizontalAlignment="Right"
            Visibility="{x:Bind ViewModel.IsMsLoginVisibility, Mode=OneWay}">
            <Button Background="Transparent" BorderBrush="Transparent">
                <PersonPicture
                    Width="36"
                    Height="36"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    ProfilePicture="{x:Bind ViewModel.MsProfilePicture, Mode=OneWay}" />
                <Button.Flyout>
                    <Flyout>
                        <MenuFlyoutItem Command="{x:Bind ViewModel.MsLogOutCommand}">Log Out</MenuFlyoutItem>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>

        <TextBlock
            Grid.Row="1"
            MaxWidth="800"
            Margin="32,16,32,32"
            Opacity="0.8"
            Text="With Graphite profiles you can separate all your browsing data. Create profiles for friends and family, or split between work and personal use."
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <Grid
            x:Name="NoUsersGrid"
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" Spacing="16">
                <FontIcon
                    HorizontalAlignment="Center"
                    FontSize="48"
                    Glyph="&#xE9CE;" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="New To Graphite" />
                <Button
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Click="StartMigration_Click"
                    Content="Start Migration"
                    Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>

        <GridView
            x:Name="UserListView"
            Grid.Row="2"
            Margin="32,0,32,32"
            HorizontalAlignment="Center"
            IsItemClickEnabled="True"
            ItemClick="UserListView_ItemClick"
            SelectionMode="None">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid
                        Width="200"
                        Height="240"
                        Margin="8"
                        Background="{ThemeResource CardBackgroundFillColorDefault}"
                        BorderBrush="{ThemeResource CardStrokeColorDefault}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Button
                            Margin="8,8,8,0"
                            HorizontalAlignment="Right"
                            Style="{StaticResource TextButtonStyle}">
                            <Button.Flyout>
                                <Flyout Placement="Bottom">
                                    <StackPanel>
                                        <ToggleSwitch Header="Sync Data" />
                                        <Button Click="Delete_Click" DataContext="{Binding}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <SymbolIcon Symbol="Delete" />
                                                <TextBlock Text="Delete User" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE10C;" />
                        </Button>

                        <PersonPicture
                            Grid.Row="1"
                            Width="100"
                            Height="100"
                            Margin="0,16"
                            DisplayName="{Binding Username}"
                            ProfilePicture="{Binding ProfileImageSource}" />

                        <TextBlock
                            Grid.Row="2"
                            Margin="16,8,16,24"
                            HorizontalAlignment="Center"
                            FontWeight="SemiBold"
                            Text="{Binding Username}" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <Grid
            Grid.Row="3"
            Padding="32,16"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefault}"
            BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button
                    x:Name="CreateNewUser"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="CreateNewUser_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE710;" />
                        <TextBlock Margin="4,0,0,0" Text="Add New User" />
                    </StackPanel>
                </Button>

                <Button
                    x:Name="OpenGuestUser"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="OpenGuestUser_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xEE57;" />
                        <TextBlock Margin="4,0,0,0" Text="Open In Guest" />
                    </StackPanel>
                </Button>

                <!--<Button
                    x:Name="RestoreBackup"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="RestoreBackup_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE72C;" />
                        <TextBlock Margin="4,0,0,0" Text="Restore Backup" />
                    </StackPanel>
                </Button>-->

                <Button
                    x:Name="WeatherButton"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="WeatherButton_Click">
                    <Button.Flyout>
                        <Flyout Placement="Top">
                            <StackPanel>
                                <ProgressRing x:Name="Loading" IsIndeterminate="True" />
                                <TextBlock
                                    x:Name="WeatherLocationText"
                                    Margin="0,0,0,8"
                                    FontWeight="SemiBold"
                                    Text="Location" />
                                <TextBlock
                                    x:Name="WeatherDescriptionText"
                                    Margin="0,0,0,8"
                                    Text="Weather Description" />
                                <StackPanel Orientation="Horizontal" Spacing="16">
                                    <TextBlock
                                        x:Name="WeatherTemperatureText"
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="Temperature" />
                                    <TextBlock
                                        x:Name="WeatherHumidityText"
                                        VerticalAlignment="Center"
                                        Text="Humidity" />
                                    <TextBlock
                                        x:Name="WeatherWindText"
                                        VerticalAlignment="Center"
                                        Text="Wind" />
                                </StackPanel>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                    <StackPanel Orientation="Horizontal">
                        <FontIcon
                            x:Name="WeatherIcon"
                            FontFamily="{StaticResource FluentIcons}"
                            Glyph="&#xE753;" />
                        <TextBlock
                            x:Name="WeatherSummaryText"
                            Margin="4,0,0,0"
                            Text="Weather" />
                    </StackPanel>
                </Button>

                <Button
                    x:Name="MigrateSys"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="MigrateSys_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE8DE;" />
                        <TextBlock Margin="4,0,0,0" Text="Migration" />
                    </StackPanel>
                </Button>
              
                <Button
                    Width="Auto"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Command="{x:Bind ViewModel.AdminCenterCommand}"
                    Visibility="{x:Bind ViewModel.IsMsLoginVisibility, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE7EF;" />
                        <TextBlock Margin="4,0,0,0" Text="Cloud Backups" />
                    </StackPanel>
                </Button>
                <Button
                    Width="Auto"
                    Margin="0,0,10,0"
                    Padding="4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Command="{x:Bind ViewModel.LoginToMicrosoftCommand}"
                    Visibility="{x:Bind ViewModel.IsLoggedIn, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="{StaticResource FluentIcons}" Glyph="&#xE716;" />
                        <TextBlock Margin="4,0,0,0" Text="Sign-In Microsoft" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <CheckBox
                Grid.Column="3"
                Margin="16,0,0,0"
                Content="Show on startup"
                IsChecked="True" />

            <InfoBar
                MaxWidth="480"
                Margin="25"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}">
                <interactivity:Interaction.Behaviors>
                    <behaviors:StackedNotificationsBehavior x:Name="NotificationQueue" x:FieldModifier="public" />
                </interactivity:Interaction.Behaviors>
            </InfoBar>
        </Grid>
    </Grid>
</Window>

