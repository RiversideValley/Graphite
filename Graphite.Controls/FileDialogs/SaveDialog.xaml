<Window
    x:Class="Graphite.Controls.FileDialogs.SaveDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Save File">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid x:Name="AppTitleBar" Height="32" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="applogo.png" Width="24" Height="24" Margin="8,0,0,0"/>
            <TextBlock x:Name="AppTitleTextBlock" Text="Save File" 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       VerticalAlignment="Center" 
                       Grid.Column="1" Margin="8,0,0,0"/>
        </Grid>

        <!-- Main content -->
        <ScrollViewer Grid.Row="1" Padding="1">
            <StackPanel MaxWidth="600" Margin="0,5" HorizontalAlignment="Center">
                <!-- Header -->
                <StackPanel HorizontalAlignment="Center" Spacing="8" Margin="0,0,0,32">
                    <Image Source="applogo.png" Width="64" Height="64"/>
                    <TextBlock Text="Save File" 
                               Style="{StaticResource TitleTextBlockStyle}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Choose a location and name for your file" 
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextAlignment="Center"/>
                </StackPanel>

                <!-- Main Card -->
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1"
                      CornerRadius="8"
                      Padding="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Save Location -->
                    <StackPanel Grid.Row="0" Spacing="8">
                        <TextBlock Text="Save Location" 
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="CurrentLocationTextBlock" 
                                       Text="Current Is Documents"
                                       VerticalAlignment="Center"/>
                            <Button x:Name="ChangeLocationButton" 
        Grid.Column="1"
        Click="ChangeLocationButton_Click">
                                <FontIcon Glyph="&#xE8B7;" FontFamily="Segoe Fluent Icons"/>
                                <ToolTipService.ToolTip>
                                    <ToolTip Content="Change Location"/>
                                </ToolTipService.ToolTip>
                            </Button>
                        </Grid>
                    </StackPanel>

                    <!-- File Name Input -->
                    <StackPanel Grid.Row="1" Spacing="8" Margin="0,16,0,0">
                        <TextBlock Text="File Name" 
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBox x:Name="FileNameBox" 
                                 PlaceholderText="Enter file name"
                                 Width="400"
                                 HorizontalAlignment="Left"/>
                    </StackPanel>

                    <!-- File Type Selection -->
                    <StackPanel Grid.Row="2" Margin="0,16,0,0" Spacing="8">
                        <TextBlock Text="File Type" 
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <ComboBox x:Name="FileTypeComboBox" 
                                  Width="200"
                                  HorizontalAlignment="Left"/>
                    </StackPanel>

                    <!-- File List -->
                    <ListView x:Name="FileListView" 
          Grid.Row="3"
          Margin="0,16,0,0"
          BorderThickness="1"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
          CornerRadius="4"
          SelectionMode="Single"
          IsItemClickEnabled="True"
          ItemClick="FileListView_ItemClick"
          Height="300"
          MaxHeight="300">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="{Binding IconGlyph}" FontFamily="Segoe Fluent Icons"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,16,0,0">
                        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click"/>
                        <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click" Style="{StaticResource AccentButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>